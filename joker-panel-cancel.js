(() => {
  "use strict";

  /*
   * Compatibility placeholder.
   * Android uses the visible cancel button and Escape handler from android-joker-v10.js.
   * The former invisible top-right pointer zone was removed to prevent duplicate cancellation.
   */
})();
